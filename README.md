# Vireonis

## Wan 2.1 video demo

The automated video demo is in `ai_video/`.

Run `ai_video/wan21_t2v_colab.ipynb` in a GPU runtime, then use its **Generate Video** button.

Output: `ai_video/output/*.mp4`.
